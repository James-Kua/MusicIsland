name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  swift-build:
    name: Swift Build
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: swift build
